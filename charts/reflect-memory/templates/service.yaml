apiVersion: v1
kind: Service
metadata:
  name: {{ include "reflect-memory.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: {{ include "reflect-memory.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
